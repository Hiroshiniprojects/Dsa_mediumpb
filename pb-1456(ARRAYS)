//Intuition
Here we are given to calculat the maximum number of vowels in a subarray.Hence this is consecutive with fixed k size we can use sliding window here.first conver the string into charecters where 1 indicates vowel and 0 indicates consonants.Build the sum logic from numeric summation and calculate.

Approach
Sliding window-calculate the inital sum of k first window loop until k
then from the k index use this technique:sum-previous+next
here get the charecter at each index outgoing and incoming then sub that and add that to the sum then calculate the max value and update the value.

Complexity
Time complexity:
O(n)

Space complexity:
O(1)
//
Code
class Solution {
    public int maxVowels(String s, int k) {
    int value;
    int sum=0;
    int max=0;
    for(int i=0;i<k;i++) {
        char ch=s.charAt(i);
        if(ch=='a'|| ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u'){
            value=1;
        }
        else{
        value=0;
        }
        sum+=value;
        
    }
    max=sum;
    for(int i=k;i<s.length();i++){
        char prev=s.charAt(i-k);
        char in=s.charAt(i);
        sum-=(prev=='a'||prev=='e'||prev=='i'||prev=='o'||prev=='u')?1:0;
        sum+=(in=='a'||in=='e'||in=='i'||in=='o'||in=='u')?1:0;
        max=Math.max(sum,max);
    }
    return max;
    }
}
